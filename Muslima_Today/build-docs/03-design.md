# 03 — Design / Layout / IA — ⚠️ SUPERSEDED

> **This doc is superseded.** The authoritative design for the build lives in:
> - **`design/02-design-system.md`** — tokens, components, motion ("Daybreak"), imagery rules.
> - **`design/03-section-specs.md`** — per-section mobile→desktop layout, the **confirmed
>   section order**, motion, assets, acceptance. Its **§0** holds the build rules; **§9** the
>   build sequence.
>
> Build from those **+ `01-content.md`** (copy). The earlier "provisional order" and component
> sketches were removed so nothing contradicts the chosen design (Direction **"Noor" (Light)**).
> Confirmed order is **persuasion-first**: Hero → About → Speakers → Testimonials →
> Past-events → **Tickets** → Footer.

## The brief's design directives (still true — quick reference) `[BRIEF]`
- **Mobile-first** — most visitors are on phones.
- **Tickets must be effortless to read** ("people can't read") — the scannable centrepiece.
- **Speaker pics: head & upper body only** (no full-length).
- **Sponsors at the bottom, in 3 tiers** (Primary / Sponsors / Media Partners).

*(Everything else — order, layouts, components, motion, colour usage, accessibility,
performance — is specified in `design/02` + `design/03`.)*
